1. Extract the complex persona "TACTILE_DIALECTICIAN" from the user prompt into a structured YAML PRP file located at `prompts/TACTILE_DIALECTICIAN_v1.0.yml` using `cat << 'INNER_EOF' > prompts/TACTILE_DIALECTICIAN_v1.0.yml`. The file will contain the exact extracted concepts, frameworks, and syntax without hallucinating unmentioned protocols.
2. Create the directory `TACTILE_DIALECTICIAN_emergence_plan` using `mkdir TACTILE_DIALECTICIAN_emergence_plan`. Inside, create `TACTILE_DIALECTICIAN_emergence_plan/strategy.md` outlining the human/AI value proposition with high surprisal feature-orientation using `cat << 'INNER_EOF' > TACTILE_DIALECTICIAN_emergence_plan/strategy.md`, and `TACTILE_DIALECTICIAN_emergence_plan/checklist.md` for rigorous implementation verification using `cat << 'INNER_EOF' > TACTILE_DIALECTICIAN_emergence_plan/checklist.md`.
3. Update `README.md` to document the new `TACTILE_DIALECTICIAN` persona and its emergence strategy using `sed` or `cat`. Ensure any observed shortcomings or laziness during this process are recorded in `LESSONS_LEARNED.md` using `cat << 'INNER_EOF' >> LESSONS_LEARNED.md`.
4. Verify the creation and modification of files using `cat prompts/TACTILE_DIALECTICIAN_v1.0.yml`, `ls -la TACTILE_DIALECTICIAN_emergence_plan`, `cat TACTILE_DIALECTICIAN_emergence_plan/strategy.md`, `cat TACTILE_DIALECTICIAN_emergence_plan/checklist.md`, `cat README.md`, and `cat LESSONS_LEARNED.md`. Re-run validation tests across the entire repository using `pip install --no-cache-dir --disable-pip-version-check yamllint==1.38.0 check-jsonschema==0.37.1`, then `yamllint .` and `find prompts -name '*.yml' -print0 | xargs -0 -r check-jsonschema --schemafile schemas/prp_schema.yml`.
5. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
6. Submit the change with a descriptive commit message using `submit` tool.
