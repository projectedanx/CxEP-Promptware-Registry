# 0xCARTO Implementation Checklist

## 1. Graph Traversal Verification
- [ ] **L1 Epistemic Graph Theory Applied**: Verify CI jobs cross-referenced against shell scripts (QP01) and Docker ENV declarations cross-referenced against code (QP03).
- [ ] **L3 Thermodynamic Entropy Mapped**: Verify orphaned dev dependencies (QP02), cyclic CI dependencies (QP07), and phantom test suites (QP15) are detected.
- [ ] **L4 Mereological Topology Extracted**: Verify developer naming sub-cultures (QP05) and temporality layers (QP06) are extracted via `git blame`.

## 2. Epistemic Validation & Constraints (PhronesisGuard)
- [ ] **No Hallucination Rule**: Ensure no missing dependencies, docs, or intent are hallucinated. If intent is ambiguous, it is documented with full tension intact.
- [ ] **Nominative Trap Detection**: Verify scripts/actions with names mismatching their actual execution flow (e.g., a "deploy" script that only lints) are strictly flagged as traps.
- [ ] **EpistemicEscrow Halt Conditions**: Ensure execution strictly halts when conditions are met (e.g., CI dependency cycle, Ground Truth Delta < 0.5, runtime falsification of script intent).

## 3. Documentation Extrusion Strictness
- [ ] **Tier 1 (Ontological Glossary)**: Verify non-standard logic/names are preserved as "Golden Scars" and not blindly standardized.
- [ ] **Tier 2 (Architecture Topology)**: Ensure Mermaid.js maps reflect actual parsed states (QP01-QP12) including orphaned infrastructure.
- [ ] **Tier 3 (CI/CD Cartograph)**: Ensure the CI/CD sequence diagram accurately captures the execution flow and highlights Nominative Traps.
- [ ] **Tier 4 (Dependency Entropy)**: Verify build reproducibility risks and entropy score are calculated and documented.
- [ ] **Tier 5 (Runbook & Scars)**: Ensure undocumented manual operational prerequisites and deep cultural artifacts are captured.

## 4. Reflexive & Security Validation
- [ ] **Bias Mitigation**: Ensure "WEIRD Normalization" is avoided by applying the 1.618 weighting for native logic preservation.
- [ ] **Schema Conformance**: Verify the generated `DRP-2026-CARTO-0.0.1` PRP adheres fully to `prp_schema.yml` constraints, including regex-validated identifiers and bounded `OUTPUT_SCHEMA`.
